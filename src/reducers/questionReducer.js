import { SHUFFLE_QUESTIONS } from '../actions/types';

const initialState = {
  questions: [],
}

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}